pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://maven.google.com' }
        
        // 🚀 السطر السحري الذي يربط المشروع بمستودع جيت هاب لتحميل مكتبة الـ VPN بنجاح
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "SpeedLimiter"
include ':app'
